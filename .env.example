# ========================================
# CONFIGURACIÓN PRINCIPAL / MAIN CONFIG
# ========================================

# Puerto del servidor / Server port
PORT=3000

# ID único del cliente (para multi-instancia) / Unique client ID (for multi-instance)
CLIENT_ID=mi-bot-empresa

# Clave de API para autenticación / API key for authentication
API_KEY=tu-clave-super-segura-aqui-cambiar-en-produccion


# ========================================
# ENTORNOS Y WEBHOOKS / ENVIRONMENTS & WEBHOOKS
# ========================================

# Entorno actual: 'production' o 'test' / Current environment: 'production' or 'test'
NODE_ENV=test

# URL del webhook para PRODUCCIÓN / Webhook URL for PRODUCTION
WEBHOOK_URL_PRODUCTION=https://api.empresa.com/webhook

# URL del webhook para TEST / Webhook URL for TEST  
WEBHOOK_URL_TEST=https://test-api.empresa.com/webhook

# Nota: El sistema usará automáticamente la URL según NODE_ENV
# Note: The system will automatically use the URL based on NODE_ENV


# ========================================
# ALERTAS TELEGRAM (OPCIONAL) / TELEGRAM ALERTS (OPTIONAL)
# ========================================

# Token del bot de Telegram para alertas / Telegram bot token for alerts
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# ID del chat donde enviar alertas / Chat ID to send alerts to
TELEGRAM_CHAT_ID=-100123456789


# ========================================
# CONFIGURACIÓN AVANZADA / ADVANCED CONFIG
# ========================================

# Timeout para operaciones de chat (ms) / Timeout for chat operations (ms)
CHAT_TIMEOUT=10000

# Límite de tamaño para archivos multimedia (MB) / File size limit for media (MB)
MAX_FILE_SIZE=10

# Reintentos automáticos en caso de error / Automatic retries on error
MAX_RETRIES=3

# Debug mode (true/false) - Muestra logs adicionales / Shows additional logs
DEBUG=false